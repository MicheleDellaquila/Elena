const MAX_TITLE_LENGTH = 100;
const MAX_FILE_SIZE = 5 * 1024 * 1024; // 5 MB
const ALLOWED_FILE_TYPES = ["image/jpeg", "image/png", "image/webp"];

module.exports = { MAX_TITLE_LENGTH, MAX_FILE_SIZE, ALLOWED_FILE_TYPES };
